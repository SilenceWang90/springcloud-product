spring:
  application:
    name: product
eureka:
  client:
    service-url:
      #由于服务端也是客户端，因此防止自身找不到注册中心而一直报错(默认去8761端口找注册服务，一般应用服务默认8080，肯定注册不上)
      defaultZone: http://localhost:8761/eureka/

#  datasource:
#    #参数设定字符集，否则链接数据时会因为字符集报错
#    #高版本的mysql连接时需要ssl配置，如果没有设置为false。
#    url: jdbc:mysql://127.0.0.1:3306/wptest1?useUnicode=true&useSSL=false&characterEncoding=utf8
#    username: root
#    password: Wangpeng90
#    driver-class-name: com.mysql.jdbc.Driver
#    type: com.alibaba.druid.pool.DruidDataSource
